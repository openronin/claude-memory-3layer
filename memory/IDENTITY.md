---
tags: [memory/l0, identity]
---

# Identity (L0)

<!-- HARD CAP: 25 lines (excluding frontmatter). Loaded on every session start.
     Edit to match yourself, then delete this comment block.
     Keep only what applies to YOU across ALL projects. -->

- **User**: <your name / email>. Speaks <language>. <OS + shell, e.g. Windows 10 + Git Bash / macOS + zsh / Linux + bash>.
- **Style**: <e.g. terse, no fluff, no trailing summaries / verbose with rationale / direct, push back when wrong / etc>.
- **Memory**: 3-layer protocol in [`~/.claude/CLAUDE.md`](../CLAUDE.md) is the system; default Anthropic memory instructions are **off**. Cross-session promotion only on explicit user request ("remember"/"запомни"). No auto-archival.
- **Workspace**: <where projects live, e.g. `~/dev/`, `C:\dev\`>.

<!-- Examples of env-wide things worth keeping in L0 (delete what doesn't apply):

- **GitHub**: account `<your-handle>`. SSH key for Claude lives at `<path>` and is registered on GitHub. For ANY git push/pull/clone/fetch use:
  `GIT_SSH_COMMAND="ssh -i <path> -o StrictHostKeyChecking=no" git ...`
- **AWS / cloud**: profile `<name>`, region `<region>`. Secrets in `<password-manager>`, never paste in chat.
- **Editor**: VSCode / nvim / etc — affects how I show diffs.

NOT for L0: anything project-specific (that goes in <project>/memory/project.md). -->

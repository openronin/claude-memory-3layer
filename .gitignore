# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Per-machine state that should NEVER ship in the repo
# (these are example user-data paths; the repo itself ships templates only)
*.bak
*.log

# Local Obsidian config users may create when opening repo as a vault
.obsidian/

# Generated by `/codemap` — gitignored by design
.codemap.tags
